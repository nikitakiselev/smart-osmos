; PlatformIO project for Smart Osmos (ESP32, Arduino framework)
; Сборка: pio run. Загрузка: pio run -t upload

[platformio]
src_dir = arduino/smart-osmos

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
upload_port = COM3
